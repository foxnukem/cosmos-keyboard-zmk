# Right half - split central. Hosts the OLED.

# ZMK's built-in status screen layout plus the left half's battery level.
# Implemented in src/custom_status_screen.c.
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y

# The built-in status screen `imply`s these; a custom one must ask for them.
CONFIG_ZMK_WIDGET_BATTERY_STATUS=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE=y
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=y
CONFIG_ZMK_WIDGET_LAYER_STATUS=y
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_LV_USE_LABEL=y

# Fonts and LVGL sizing, likewise only defaulted for the built-in screen.
# Montserrat 12 (the small font) comes from ZMK_LV_FONT_DEFAULT_SMALL.
#
# 14, not the built-in screen's 16, because the panel is only 32px tall and both
# battery readings sit in the default font. Montserrat 14 has a 16px line height,
# so the two rows tile exactly into 32px with ~6px of clear space between the
# glyphs; at 16 (18px lines) the rows overlap and only avoid colliding because
# digits and '%' have no descenders.
CONFIG_LV_FONT_MONTSERRAT_14=y
CONFIG_LV_FONT_DEFAULT_MONTSERRAT_14=y
CONFIG_LV_USE_THEME_MONO=y
CONFIG_LV_Z_MEM_POOL_SIZE=4096
